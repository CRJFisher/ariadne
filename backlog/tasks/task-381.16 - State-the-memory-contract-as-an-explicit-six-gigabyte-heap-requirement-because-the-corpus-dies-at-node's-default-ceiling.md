---
id: TASK-381.16
title: "State the memory contract as an explicit six-gigabyte heap requirement, because the corpus dies at node's default ceiling"
status: To Do
assignee: []
created_date: "2026-08-24 09:07"
labels:
  - memory
  - performance
  - docs
dependencies:
  - TASK-381.8
  - TASK-381.11
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->

Every full-corpus run in this investigation used `--max-old-space-size=12288`, and nobody had run the corpus without the flag. A user who types `ariadne` on a 16 GB laptop either gets an entry-point report or gets `Ineffective mark-compacts near heap limit`, and which one they get depended on a flag they have no reason to know exists. That question is now answered by measurement, and the answer is that the default does not suffice.

At node's default old-space ceiling on this machine — 4,144 MB on a 16 GiB Darwin 21.6.0 box running node v22.23.2 — the repaired load OOMs after 666 s of CPU with `FATAL ERROR: Ineffective mark-compacts near heap limit`, its last GC at 4,047.7 of 4,133.9 MB, a single mark-compact of 6,178 ms recovering 0.4 MB, mutator utilisation 0.005. The live heap settles at 3,563.8 MB, so V8 is left with no mark-compact working set at all. `--max-old-space-size=6144` completes: 8,494 of 8,494 files, 474,838.4 ms of CPU (+2.6% against the 12 GB runs), peak RSS 4,172.0 MB, and fingerprints byte-identical to the 12 GB runs. Independently, the stack composed with TASK-381.11 also completes at 6144 — 507.0 s, peak RSS 5,367.4 MB, all five fingerprint hashes byte-identical to its 10 GB runs. So the contract is a floor of 6 GB, and it is a hair rather than a chasm: the shortfall is between the 4,144 MB default and the 6,144 MB that works, and it is GC working set rather than retained data.

The resolution must not be a flag Ariadne sets for itself. Setting `--max-old-space-size` from inside the CLI requires a re-exec or a `NODE_OPTIONS` hand-off — a second execution path, which the constitution forbids — and it would cover only the CLI, leaving the MCP server and the library consumer with neither the flag nor the guarantee. So the outcome of this task is a documented requirement plus a follow-up task against the measured shortfall, not a runtime workaround. Two figures belong in that documentation because they are what future memory reasoning will get wrong otherwise. The RSS-to-settled-heap ratio is not a constant: it was 2.3x before the export-gate repair (7.83 GB resident against 3.32 GB settled) and 1.17x after (4,172.0 against 3,563.8 MB), because the native tree-sitter arenas that inflated it were being fed by files that got indexed and thrown away. And peak RSS varies up to 26% run to run on identical inputs while settled heap is stable to 0.01%, so any RSS number stated as a single run's figure will flap.

## What landed

The contract is re-measured on the landed stack, ariadne@417de2fc, on a 6-core Darwin 24.6.0 box with 32 GiB and node v22.22.1 — a different machine with the same 4,144 MB default ceiling. Every verdict holds and every absolute moves. The corpus dies at the default ceiling after 444.3 s of CPU with `Reached heap limit Allocation failed`; `--max-old-space-size=6144` completes in 356.22 s of CPU as a mean of two processes, +1.96% against an interleaved 12,288 MB control, at 5,803.45 MB peak RSS, and reports a call graph byte-identical to the control's and to `RECORDED_ORDER_INDEPENDENCE`.

Two figures are sharper than the task was written with. The live heap is 4,046.1 MB — 97.9 MB below the default ceiling rather than 580 MB below it — so the collector is left even less working set than the original reading suggested, and TASK-393 carries that shortfall. And the RSS-to-live-heap ratio is a pair here for a different reason: what moves it on this box is the CEILING (1.43x at 6,144 MB against 1.70x at 12,288 MB over one identical live set), not the export gate, which `RECORDED_EXPORT_DECLARATION_SPACE` measures at -0.03% peak RSS here. The same reading also corrects "settled heap is stable": the closing `used_heap_size` is the live set only where the ceiling forces a full collection — 0.01% apart at 6,144 MB, 24.84% apart at 12,288 MB.

The contract lives in `RECORDED_MEMORY_CONTRACT` and in `packages/core/src/benchmark_corpus_load/README.md`, and Ariadne still sets no heap flag anywhere.

<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria

<!-- AC:BEGIN -->

- [ ] #1 A full-corpus cold load of every discovered file under vscode `src/` is run with NO `--max-old-space-size` flag and recorded as completing or not completing. MEASURED: does NOT complete. Dies after 666 s of CPU with `FATAL ERROR: Ineffective mark-compacts near heap limit` at a 4,144 MB default ceiling, last GC 4,047.7 of 4,133.9 MB, mark-compact 6,178 ms, mu 0.005. RE-MEASURED on landed code (ariadne@417de2fc, Darwin 24.6.0 x64, 6 cores, 32 GiB, node v22.22.1, tree-sitter 0.25.0, all 8,494 discovered files offered to one process, forward order): still does NOT complete, at the same 4,144 MB default ceiling. It dies after **444.3 s of CPU** (372.6 s wall) with `FATAL ERROR: Reached heap limit Allocation failed - JavaScript heap out of memory`. Its last six mark-compacts each run 1,236-2,259 ms to free 9-53 MB, the last one 4,022.6 -> 4,014.0 MB against a 4,150.0 MB committed heap at mu 0.018 current / 0.049 average. The last progress the arm reported was at 301 s of wall with a 3,035.9 MB used heap and 3,790.5 MB resident; it never reached the trace phase, so there is no fingerprint for this arm and that absence is the result.
- [ ] #2 The shortfall is recorded as a bracket rather than a guess, and the flag that closes it is measured. MEASURED: 4,144 MB fails and 6,144 MB completes — 8,494/8,494, 474,838.4 ms CPU (+2.6% vs 12,288 MB), peak RSS 4,172.0 MB, settled heap 3,563.8 MB, fingerprints byte-identical to the 12 GB runs. The composed stack with TASK-381.11 also completes at 6,144 MB: 507.0 s, peak RSS 5,367.4 MB, all five hashes byte-identical to its 10 GB runs. RE-MEASURED on landed code, both halves of the bracket in ONE session interleaved A,B,A,B (sequence 1-4, forward order, every discovered file, one process per arm): **4,144 MB fails and 6,144 MB completes**. The 6,144 MB arms index 8,494/8,494 with an empty dropped set in **356.22 s of CPU** (mean of 2, spread 0.63%, reps 355.10 / 357.34), which is **+1.96%** against the 12,288 MB control arms interleaved with them (349.37 s, reps 347.61 / 351.12), at **5,803.45 MB peak RSS** (mean of 2, spread 1.42%) and a 5,155.25 MB settled heap (spread 0.01%). All seven fingerprint components and both diagnostics hashes are byte-identical across all four arms — nodes 201595/1dee6f73bd6b19b3, resolved 1077986/1ddc158820141bce, unresolved 420958/4783fb8da9030c81, eps 17563/81190da4a3cade3d, indirect keys 29378/bd658514f967310e, dropped 0/e3b0c44298fc1c14, evidence 29378/0d66eb1473576544, canonical 834cc16d32aef077, diag d08f8e814597b4bb — and equal to `RECORDED_ORDER_INDEPENDENCE`'s digests for this tree, so the ceiling changes the collector's schedule and nothing the user is shown.
- [ ] #3 NO heap flag is added anywhere in Ariadne — no re-exec, no `NODE_OPTIONS` hand-off, no CLI-only path — and the requirement is documented instead, naming the machine it was measured on (Darwin 21.6.0, 4 cores, 16 GiB, node v22.23.2), the corpus (microsoft/vscode f3fa55c3, `src/`, 8,494 discovered files) and the Ariadne commit. A follow-up task is opened against the measured shortfall: get the live heap far enough below 4,144 MB that the default ceiling has a mark-compact working set, from a measured 3,563.8 MB today. LANDED: **TASK-393** carries that shortfall, written against a live heap of **4,046.1 MB** — the mean of three forced-collection readings, spread 0.01%, taken at both ceilings that complete — which is **97.9 MB below** the 4,144 MB default rather than 580 MB below it, so the working set the collector is left is smaller than the figure this task was written from. The no-flag half is re-audited on landed code: `--max-old-space-size`, `max_old_space`, `NODE_OPTIONS`, `process.execPath` and `setFlagsFromString` match **zero** times outside `src/benchmark_corpus_load/**` and `packages/core/scripts/run_load_benchmark.ts`, both of which are the measurement harness and are excluded from `tsconfig.build.json`; the published CLI's shebang is a plain `#!/usr/bin/env node` and no package.json carries a node flag. The requirement is documented in `packages/core/src/benchmark_corpus_load/README.md` and in `RECORDED_MEMORY_CONTRACT`, naming the machine (Darwin 24.6.0 x64, 6 cores, 32 GiB, node v22.22.1), the corpus (microsoft/vscode f3fa55c3, `src/`, 8,494 discovered files) and the Ariadne commit (417de2fc).
- [ ] #4 REFUTES this epic's `<= 5 GB peak RSS at node's default heap` commitment in both halves. Peak RSS for the composed stack is 5,367.4 / 5,410.5 / 5,583.6 / 5,930.8 / 6,510.6 MB (mean 5,760.6 over 5 runs at a 10,240 MB cap), and the default heap does not run at all. The replacement contract is: explicit `--max-old-space-size >= 6144`, peak RSS <= 6.6 GB stated as a mean of >= 2 runs. RE-MEASURED: the first half stands and the `<= 6.6 GB` bound is itself REFUTED as a portable figure, because peak RSS is bounded by the ceiling the collector is given rather than by the corpus. On landed code over one identical live set: **5,803.45 MB at a 6,144 MB ceiling** (5,762.2 / 5,844.7, mean of 2) and **6,880.9 MB at 12,288 MB** (7,058.0 / 6,703.8, mean of 2). So the bound is stated per ceiling — <= 6.0 GB at 6,144 MB and <= 7.1 GB at 12,288 MB, each a mean of two runs — and a peak RSS figure quoted without its ceiling means nothing. The default heap still does not run at all.
- [ ] #5 The RSS-to-settled-heap ratio is recorded as a MEASURED PAIR and not as a constant, so no future memory projection in this codebase is made from heap alone: 2.3x before the export-gate repair (7.83 GB resident / 3.32 GB settled) and 1.17x after (4,172.0 / 3,563.8 MB). The reason for the change is stated — the native tree-sitter arenas that inflated the ratio were being fed by 603 files that were indexed and then discarded. RE-MEASURED on landed code, and the ratio is a pair here too while the reason it moves is a different one. Over one live set of **4,046.1 MB**, identical to a tenth of a megabyte at both ceilings, peak RSS gives **1.43x at a 6,192 MB cap** (5,803.45 MB) and **1.70x at 12,336 MB** (6,880.9 MB): the collector schedules against the ceiling it is given, so resident memory follows the flag rather than the corpus. The export-gate half of the original pair does not reproduce on this box at all — `RECORDED_EXPORT_DECLARATION_SPACE` measured the repair at 6,911.75 MB candidate against 6,914.1 MB control, -0.03% — so the 2.3x -> 1.17x move is carried as the 4-core box's reading rather than as the mechanism. A second correction belongs with it: the closing `used_heap_size` is the live set ONLY where the ceiling forces a full collection. At 6,144 MB two processes read 5,155.1 and 5,155.4 MB (0.01% apart); at 12,288 MB the same computation reads 5,592.4 and 7,178.8 MB (24.84% apart). So no RSS-to-heap ratio may be quoted from a run whose ceiling never forced a collection, and the live figure is taken under `--expose-gc` rather than read off the end of the run.
- [ ] #6 Any RSS criterion anywhere in this epic is a mean over >= 2 independent runs, because peak RSS varies up to 26% run to run on the same arm and inputs (4,430.4 vs 5,952.5 MB on one control arm; 4,194.1 vs 5,040.5 on the repair) while the settled post-GC heap is stable to 0.01%. AUDITED across every task doc in this epic and satisfied: TASK-381.1 #13 states the rule and `summarize_samples` refuses fewer than two runs; TASK-381.7 #3 is 3 processes over `src/` and 2 over the repository root; TASK-381.8 #5 is 2 per arm; TASK-381.11 #10 is 2 per arm; TASK-381 #3 and this task's #2, #4 and #7 are means of 2 per ceiling. The single-run figures this task was written from — 5,367.4 MB at 6,144 MB and 4,172.0 MB — are carried as one 4-core box's readings and are not criteria. The second half is CORRECTED: the settled heap is stable to 0.01% only where the ceiling forces a full collection (0.01% at 6,144 MB against 24.84% at 12,288 MB over one identical live set), so "settled heap" without its ceiling is not a stable figure either. Measured run-to-run peak RSS spread on this box: 1.42% at 6,144 MB and 5.15% at 12,288 MB.
- [ ] #7 The 6 GB floor is stated for the `src/` corpus only, with the repo-root figure recorded beside it: pointed at the repository root (12,654 discovered files) the same stack peaks at 7,492.8 MB with a 12,288 MB cap, so 6,144 MB is NOT known to be sufficient there and the documented requirement must say which corpus it holds for. RE-MEASURED on landed code, two independent processes at a 22,645 MB cap in the same session: the repository root indexes **12,653 of 12,654** (the one drop is TASK-387's scope-tree invariant, not a memory effect, and both arms report one identical fingerprint) in **1,151.69 s of CPU** (mean of 2, spread 0.38%), at **8,540.05 MB peak RSS** (mean of 2, 8,753.5 / 8,326.6, spread 5.0%) over a live heap of **5,562.95 MB** (mean of 2, spread 0.01%). So 6,144 MB stays NOT known to be sufficient there, and now for a stated reason: a 6,144 MB ceiling would leave 581 MB of collector working set over that live heap, where `src/` died with 97.9 MB. The floor is stated for `src/` in `RECORDED_MEMORY_CONTRACT`, in `packages/core/src/benchmark_corpus_load/README.md` and in the requirement sentence itself, each naming the corpus, and the repo-root row sits beside it with its own ceiling.

<!-- AC:END -->
