---
id: TASK-381.11
title: "Make the reported call graph a function of the codebase rather than of the order files were ingested"
status: To Do
assignee: []
created_date: "2026-08-24 09:07"
labels:
  - call-resolution
  - determinism
  - bug
  - call-graph
dependencies:
  - TASK-381.8
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->

The same file set in a different ingest order produced a different call graph, on the unpatched build as much as on everything this epic adds. The mechanism has now been found, and it is not where the earlier hypothesis put it. `fix_import_definition_locations` (`packages/core/src/project/fix_import_locations.ts:66`) rewrites every `ImportDefinition`'s `location` to the exact location of the definition it names in the other file, so that "go to definition" on an imported symbol jumps to the declaration. `DefinitionRegistry.update_file` then keyed `location_to_symbol` on `location_key(def.location)` for EVERY definition including imports (`registries/definition.ts:116-117`), so one symbol's declaration location was claimed by the declaration and by every file that imports it; the map holds one value, so the survivor was whichever file was written last. `remove_file` (`:355-356`) made it worse by deleting the declaration's key outright when an importer was evicted.

Nothing about that is order-dependent on the incremental `update_file` path, which is why it survived so long. It bites only on the BULK path: `resolve_corpus` (`project/project.ts:204-222`) runs Phase 2.5 for every file before `resolve_files` runs for any, so by the time Phase 4 asks `definitions.get_symbol_at_location` for a constructor binding (`registries/type.ts:155`) the key may answer with an importer's import symbol instead of the declaration. The type is then bound to the import symbol, `types.get_symbol_type(declaration)` is null forever, and every `x.method()` through that symbol fails at `receiver_type_unknown` (`call_resolution/receiver_resolution.ts:285`), making its methods look unreachable. The affected shape is vscode's exported-singleton idiom — `export const extUri = new ExtUri(...)`, `export const IME = new IMEImpl()`, `export const Registry: IRegistry = new RegistryImpl()` — imported and used from more than one file. A ten-line synthetic case pinned it before any product code was touched: same three files, two orders, `symbol_at_decl_location` the declaration in one and an importer's import symbol in the other, 0 versus 2 resolved edges.

A second, independent order-dependence was found only by adding a seventh number to the fingerprint. `IndirectReachability` is keyed by the reachable function, but many files can read that function as a value and only one read site is stored as the reported evidence; four writers each picked by walk order — three sites in `indirect_reachability.ts` (last-wins), `callable_value.ts:41` (first-wins), the `call_resolver.ts:143` merge (first-wins) and `resolution_state.ts:290` (last-wins). This never moved entry-point membership, which is exactly why the six-number fingerprint could not see it. The fix is a `def.kind !== "import"` gate on the location index in both `update_file` and `remove_file` — the same carve-out `by_scope` already makes two lines below, and all four readers of that index want a declaration — plus a single writer `record_indirect_reachability(into, fn_id, entry)` that keeps the read site earliest in the project by file path, then line, then column, with every competing writer routed through it.

The earlier diagnosis in this task is REFUTED and must not be planned against. `by_symbol` insertion order in `get_callable_definitions` (`call_resolver.ts:456`) is not part of the answer: four ingest orders necessarily produce four different insertion orders and, after the fix, one byte-identical fingerprint. The prototype that appeared to implicate it moved the graph because re-inserting only a file's imports changed which symbol won a shared `location_to_symbol` key. The phase-2 suspicion of state accumulated inside `resolve_type_metadata` or `extract_constructor_bindings` is also refuted: the sensitivity is created in Phase 2.5, before a single name is resolved, and resolution was faithfully reading a corrupted index.

<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria

<!-- AC:BEGIN -->

- [ ] #1 An `ImportDefinition` never enters `DefinitionRegistry.location_to_symbol`: `update_file` and `remove_file` both gate on `def.kind !== "import"`, with the reason stated in the code — `fix_import_definition_locations` (`project/fix_import_locations.ts:66`) gives an import the location of the definition it names, so N importers of one symbol otherwise claim that symbol's key and the last writer, i.e. the ingest order, wins. Both indices the gate covers are gated in BOTH directions: `update_file` already kept imports out of `by_scope` while `remove_file` deleted on their behalf, so eviction now inverts insertion exactly rather than half of it.
- [ ] #2 One reachability entry per function, chosen by position in the project (file path, then line, then column), through the single writer `record_indirect_reachability` (`resolve_references/indirect_reachability.ts`). All six competing writers route through it: the three sites in `detect_indirect_reachability`, `resolve_callable_values` (`callable_value.ts`), the call-resolver merge (`call_resolver.ts`) and `apply_call_resolution` (`resolution_state.ts`). Two were first-wins and two last-wins before. The comparison runs on past the start position — end line, end column, then reason type, then collection id — because start position alone is not a total order and a pair it leaves unordered is a pair the walk decides: two references share a start where `apply_twice` is read both as the callee name and as the whole call expression, and one read of a collection that spreads another reaches the same function under two collection ids at one location.
- [ ] #3 A SEVEN-number fingerprint — nodes, resolved call edges, unresolved call sites, raw entry points, the `indirect_reachability` key set, the dropped-file set, and the evidence tuple behind each indirect reachability (fn id, reason type, collection id, read site) — is byte-identical across ingest orders. MEASURED on the landed stack at the 1,200-file slice of vscode's `src/` in forward, reverse, descending-byte-size and mulberry32 shuffles seeded 7 AND 13, both reproducing the same digests: nodes 28057/d0a70789f48a4e2f, resolved 84346/b32e61fb8179dbcc, unresolved 62942/6cb76597cbe63c25, eps 4141/50307354dde86797, indirect keys 4450/7c25d1cf1ffc8c32, dropped 0/e3b0c44298fc1c14, indirect evidence 4450/4540f71f3ad37723 — plus one diagnostics `canonical_hash` b89fb63905853697. Before the fix the same four orders gave FOUR different entry-point sets (4163 / 4160 / 4156 / 4162, union 4168 against intersection 4151) and four different evidence hashes. The prototype's figures (eps 4301/3b2ddde989394afb, nodes 24781, refs 200574, resolved 78073, indirect 3250, set hash 156e145a8df2f60a) are SUPERSEDED: they predate TASK-381.8's readmission of 676 files and came from a five-component fingerprint on a different box.
- [ ] #4 The same identity holds at FULL corpus. MEASURED over all 8,494 discovered files, four processes, forward / reversed / descending-byte-size / mulberry32 seed-7 shuffle, 8,494 indexed and 0 dropped in every one: nodes 201595/1dee6f73bd6b19b3, resolved 1077986/1ddc158820141bce, unresolved 420958/4783fb8da9030c81, eps 17563/81190da4a3cade3d, indirect keys 29378/bd658514f967310e, dropped 0/e3b0c44298fc1c14, indirect evidence 29378/0d66eb1473576544, diagnostics canonical 834cc16d32aef077. Recorded in `RECORDED_ORDER_INDEPENDENCE`. Residue: the diagnostics `diag_hash` takes four values under that one canonical hash — the payload's membership is settled and its emission order is not — and it reads the same way on the pre-fix tree, so it is neither introduced nor removed here. The prototype's figures (eps 16055, nodes 195087, refs 1545264, resolved 1481123, indirect 29363) are SUPERSEDED for the same reason as #3.
- [ ] #5 NON-VACUITY, mandatory: the determinism probe must be shown to move on a known order-dependent tree before its silence on the fixed tree counts. MEASURED IN THIS SESSION on ariadne@7a7d99b0, the tree this change was made on. Full corpus, forward against descending byte size over the identical 8,494 files: entry points 17,647 -> 17,620 (32 forward-only, 5 revsize-only = 37 moved), resolved edges 1,076,088 -> 1,076,137 (136 / 185), unresolved 424,057 -> 424,063 (322 / 328), and four of the seven components change hash — resolved 9ebbb24c04c88952 -> 3d67874d14d450c3, unresolved bc249186fe32e982 -> c0155e5401833b75, eps 4812efbaee47ef27 -> bf5f0271360d42fb, indirect evidence 81d3bb3c789f39a6 -> e4d81e48eb2bfe23 (2,679 members each way) — while nodes 1dee6f73bd6b19b3, indirect keys f32e80ad5909e494 and dropped e3b0c44298fc1c14 stay identical. The diagnostics `canonical_hash` moves too (07dd6824aa8a0713 -> 872d20a3b82b3452), which is a membership difference. At 1,200 files the same tree gives four different entry-point sets. The moved entry points cluster on the diagnosed idiom: ime.ts, inputMode.ts, tabFocus.ts, onboardingRegistry.ts, implicitActivationEvents.ts, textAreaEditContextRegistry.ts. The 17,994 -> 17,973 / 31-moved figures in `RECORDED_ORDER_SENSITIVITY` are an OLDER tree's demonstration and are kept beside this one, not as this one.
- [ ] #6 The converged answer is more correct, not merely stable. MEASURED at full corpus by literal set difference over the complete member lists, not from the counts: the entry-point set is a strict SUBSET of the pre-fix set (-84, +0), the resolved caller-to-callee edge set a strict SUPERSET (+1,898, -0) with resolved call sites 1,291,072 -> 1,294,238 and 98 surviving pairs gaining call sites, unresolved call sites a strict SUBSET (-3,099, +0), indirectly-reachable functions +3/-0 — exactly `platform.ts` Registry `add`, `knows` and `as` — and the node set byte-identical, 201,595 either way. Every removed entry point is a method of the exported-singleton idiom; spot-verified at the call site in the corpus source: `ime.ts:15 enabled` <- `screenReaderContentSimple.ts:122` (`!IME.enabled`, inside the anonymous function at :120); `tabFocus.ts:14 getTabFocusMode` <- `editorConfiguration.ts:131` inside `_computeOptions` at :116; `errors.ts:64 onUnexpectedError` <- `errors.ts:107`; `platform.ts:48 as` <- 514 distinct callers. Three of the 84 — `taskDefinitionRegistry.ts` `onReady`:151, `get`:155, `getJsonSchema`:163 — are part of TASK-389's recorded population, which this change resolves. The prototype's -73/+1,217 are SUPERSEDED by the numbers above.
- [ ] #7 Three guards, each verified to FAIL on the pre-fix tree and pass after — a guard that passes on the broken build does not count. (a) `registries/definition.test.ts`: a declaration plus two importers carrying its rewritten location, written in order; the declaration still answers `get_symbol_at_location`, and evicting an importer does not delete its key. (b) `indirect_reachability.test.ts`: the earliest read site is recorded whichever file the walk reaches first, two sites in one file are ordered by position (asserted in BOTH directions, because first-wins answers an ascending list by luck and last-wins a descending one), and a later writer merging into the map does not displace it. (c) `resolve_references.typescript.test.ts` "TypeScript Ingest Order Independence": three files driven through the BULK path (`ingest_file` x N then `resolve_corpus`) in three orders give one identical call-graph shape, with two named edges asserted so an agreed-empty graph cannot pass. The bulk path is MANDATORY — the same test written against `Project.update_file` passes on the broken build. MEASURED: 6 assertions fail on the pre-fix tree and 4,167 of 4,176 tests pass there, against 4,176 of 4,176 after, so no other test changes state. Three further tests fail pre-fix because they were re-baselined by this step, not added by it: the in-repo corpus guard's member list and hash derivation, and `benchmark_corpus_load.test.ts`'s order-dependence guard, which was written to turn red when this landed and now asserts independence.
- [ ] #8 REFUTED, and this epic must stop planning against it: `by_symbol` insertion order is NOT part of the answer. Four ingest orders necessarily produce four different insertion orders and, post-fix, one byte-identical fingerprint. `get_callable_definitions` (`call_resolver.ts:456`) needs no ordering rule and no tie-break, and the original criterion demanding one is WITHDRAWN. The `patch_import_locations` prototype moved the graph because re-inserting only a file's imports changed which symbol won a shared `location_to_symbol` key.
- [ ] #9 REPLACES the original positional criterion (a 180-file set with `resources.ts` at position 24 and at position 155). The binding is pinned instead by a ten-line synthetic reproduction — a declaration plus two importers, two orders — asserting the declaration answers `get_symbol_at_location` and `symbol_types` holds the class in both, plus the bulk-path integration guard above. The 180-file positional harness is recorded as superseded, not deleted for convenience: it demonstrated the symptom but not the stage.
- [ ] #10 Cost is stated, not hidden. MEASURED at full corpus from arms interleaved A,B,A,B in ONE session on ONE box, both forward order over all 8,494 files: before 338.8 / 345.4 s (mean 342.1, spread 1.9%), after 353.2 / 355.2 s (mean 354.2, spread 0.6%) = 1.035x, +3.5%. That is outside the within-arm spread, so it is a real cost and is stated as one rather than waved off as noise. What it buys is named: 4,941 call sites stop bailing at `receiver_type_unknown`, 3,108 of them resolve and 1,842 reach member lookup and fail there instead. Peak RSS before 7,371.4 / 7,432.4 MB (mean 7,401.9), after 7,188.6 / 6,924.6 MB (mean 7,056.6) — lower after, under a 3.8% within-arm spread, so no memory cost is claimed either way. Machine: Darwin 24.6.0 x64, 6 cores, node v22.22.1, tree-sitter 0.25.0, heap cap 15,365 MB, loadavg 4.3-5.1 at arm start, cpu/wall 1.04-1.07. The prototype's 876,859 / 920,248 ms and its RSS pair are SUPERSEDED: absolute CPU does not transfer between sessions or machines, and only the same-session ratio does.
- [ ] #11 `receiver_type_unknown` counts over the full corpus are recorded before and after, so the size of the remaining type-inference gap is a number rather than an impression. MEASURED over all 8,494 files, one process per tree: 163,174 -> 158,233 (-4,941, -3.03%). The whole taxonomy is recorded beside it, because the fall is only meaningful against what did not move: `name_not_in_scope` 165,901 unchanged, `member_type_unknown` 59,544 -> 60,065, `method_not_on_type` 23,339 -> 24,660, and `polymorphic_no_implementations`, `class_definition_not_found`, `collection_dispatch_miss`, `no_enclosing_class_scope`, `constructor_target_not_a_class` and `no_parent_class` all unchanged. Unresolved call sites 424,057 -> 420,958 against 977,985 -> 977,994 references. `receiver_type_unknown` remains the second-largest failure class after `name_not_in_scope`, at 37.6% of all unresolved sites.
- [ ] #12 Two order-dependent mechanisms remain open and are recorded rather than fixed here, each with a follow-up: `remove_files` (`resolution_state.ts:239`) evicts an indirect-reachability entry by the file of the ONE recorded read site, so re-indexing that file drops the entry even when other files still reach the function — invisible to bulk load, live on the incremental edit path — carried by TASK-391; and `TypeRegistry.register_late_binding` (`registries/type.ts:331`, called from `call_resolver.ts:309`) mutates the type registry DURING Phase 5, so a binding registered while resolving one file is visible to files resolved later in the same batch — it moved no fingerprint on this TypeScript corpus but is order-dependent by construction and would bite a Python corpus — carried by TASK-392.
- [ ] #13 A THIRD residue found by this step's own measurement is recorded rather than absorbed: the position rule that makes the evidence order-independent also makes an import specifier win it, because importing a callable is a read of it and an import statement sits above every use in its file. Over the ten-file in-repo corpus, eight of ten evidence tuples now name an import specifier and three of those report `function_reference` where the collection holding the function is the informative answer. Membership does not move — `indirect_reachability_keys` is +3/-0 at full corpus and the entry-point set is a strict subset — so this is evidence quality, not reachability. Carried by TASK-390.

<!-- AC:END -->
