---
id: TASK-381.17
title: "Index files across worker threads so a full corpus finishes in minutes of wall clock on a multicore box"
status: To Do
assignee: []
created_date: "2026-08-24 09:07"
labels:
  - performance
  - architecture
dependencies:
  - TASK-381.9
  - TASK-381.10
  - TASK-381.11
  - TASK-381.13
  - TASK-381.14
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->

Pass A of the two-phase driver is embarrassingly parallel: `parse_file` and `build_index_single_file` touch no registry. The seam already exists — `SemanticIndex` is what the persistence cache serialises, so `serialize_semantic_index` and `deserialize_semantic_index` are a ready-made transport and `Project.restore_file` (`packages/core/src/project/project.ts:173`) a ready-made apply path, proved fingerprint-identical to `update_file` across all six hashes over 191 real vscode files — and tree-sitter's native binding loads per worker thread with no special handling.

The unit changes here and the criteria change with it. Threads trade CPU for wall: the one pool that was actually built measured wall 16,607 to 15,084 ms while CPU rose 19,094 to 25,454 ms, up 33%, at n=200. That measurement was taken when resolution still dominated the run and it does not transfer, which is why the wall target below is COMPUTED from a re-measured share rather than fixed in advance. Every other item in this epic is judged on CPU because wall on a shared box is scheduling; this one is judged on wall taken on an idle box with CPU reported alongside, and never booked as a CPU saving.

Three constraints were learned the hard way. The pool must preserve caller order, because the reported graph is order-dependent — TASK-381.11 is what makes that structurally safe rather than merely arranged. Use the JSON transport and not `structuredClone`, which throws on any residual non-cloneable field and takes the whole file down: pre-fix it lost 133 nodes, 172 call edges and 15 entry points on `vs/base` alone, which is what TASK-381.10 removes. And on a contended machine the pool is a net loss — at loadavg 7-19 on four cores every pool arm was slower than serial, wall up 21% and CPU up 24-31%, with cpu/wall at 0.97 while three workers ran, meaning they took no extra CPU and stole it from the critical path. The answer to that is one mechanism with a computed width, not a pool path beside a serial path: width comes from cores and load, and a width of one is the same dispatch code running a single worker.

The pool is also what makes post-load's one parallel seam available. The indexed grep pass and the residue grep pass are two textual byte-passes over disjoint file sets sharing no state but their output Map, and they dispatch through this pool rather than growing a second threading mechanism of their own. This item lands last because it is the only one whose value depends on everything before it having already changed the shape of the run.

<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria

<!-- AC:BEGIN -->

- [x] #1 #1 Before any pool code is written, the parallelisable share of the post-TASK-381.8 full-corpus run is re-measured and recorded, and the wall target for AC #3 is COMPUTED from it — not taken from the n=200 prototype and not fixed in advance. MEASURED on ariadne@2b13f344, the tree immediately before this step, by splitting one whole-corpus load at the seam a pool cuts at: 8,494 of 8,494 files, 227.41 s of CPU over 217.88 s of wall at cpu/wall 1.044, of which parse-and-index is **178.01 s — a share of 0.7828**. The rest is read 20.15 s, populate registries 5.45 s, `resolve_corpus` 22.89 s, initialize 0.34 s and trace 0.54 s. The split arm's seven components are byte-identical to `RECORDED_ORDER_INDEPENDENCE`, so the decomposition is the serial arm and not a different one. The target for AC #3 is `serial_wall x (1 - 0.7828 + 0.7828/3.2)` = **97.99 s** against a same-session serial control mean of 212.19 s. Recorded in `RECORDED_WORKER_INDEX_DISPATCH`.
- [x] #2 #2 Pass A dispatches to a worker pool over the JSON transport, results are applied in caller path order, and the six-number fingerprint is identical to the serial arm at n=200, n=1,200 and over the full corpus. The worker returns its `SemanticIndex` as a JSON string; the main thread applies each file through `ingest_restored_file` in `files_to_load` order, out-of-order results buffering until their turn. MEASURED identical on ALL SEVEN components and BOTH diagnostics hashes at n=200, n=1,200 and n=8,494, at width five and at width one, against the serial arm run from a second checkout in the same session: nodes 201595/1dee6f73bd6b19b3, resolved 1077986/1ddc158820141bce, unresolved 420958/4783fb8da9030c81, entry points 17563/81190da4a3cade3d, indirect keys 29378/bd658514f967310e, dropped 0/e3b0c44298fc1c14, evidence 29378/0d66eb1473576544, diag d08f8e814597b4bb, canonical 834cc16d32aef077.
- [x] #3 #3 Full-corpus cold WALL on an otherwise-idle 4-core box meets the target computed in AC #1 as `serial_wall x (1 - share + share/3.2)`, with 3.2 stated as the measured 4-core efficiency of the pool at full corpus; the computed target and the achieved wall are both recorded. Total CPU is reported alongside and explicitly permitted to exceed the serial arm's by up to 35% (measured +33% at n=200). No CPU reduction is claimed. MEASURED on a 6-core box, not a 4-core one, and the box was the least loaded this session could get rather than idle — every arm carries its loadavg and cpu/wall. Four arms interleaved control,candidate,control,candidate in one session: serial (ariadne@2b13f344) 208.12 and 216.26 s of wall, **mean 212.19 s**, 222.94 and 230.35 s of CPU, mean 226.65 s; pooled at width five (ariadne@ecca77cd) 79.98 and 79.32 s of wall, **mean 79.65 s against the 97.99 s target**, 277.08 and 276.18 s of CPU, mean 276.63 s. That is **2.664x on wall and 1.2206x on CPU — a 22.06% rise inside the 35% permitted**, and no CPU reduction is claimed. Back-solving the achieved wall gives this box an efficiency of **4.95** rather than the criterion's 3.2, because the per-file read the share left on the main thread moved into the workers too.
- [x] #4 #4 There is one dispatch mechanism: worker width is computed from core count and loadavg, a width of one runs the same code with a single worker, and a contended run is never slower than the width-one arm — against every pool arm today running +21% wall and +24-31% CPU at loadavg 7-19. The width is `max(1, min(cores - 1, floor(cores - loadavg[0])))`; one core always stays with the main thread, which deserializes every result and resolves the corpus. A width of one is the same `dispatch_to_workers` call with one worker — there is no serial path beside it. MEASURED: at loadavg 7.26 on six cores the rule computed **width one**, so the contended run IS the width-one arm and cannot be slower than it. The prototype's contended finding is REFUTED on this box and carried as a correction: at the same load an explicit width-five arm ran **141.55 s of wall against 327.17 s** for the computed width-one arm, at 281.78 s of CPU against 274.86 s — faster in wall and level in CPU. What it does is claim six of twelve runnable threads instead of two of eight, which is taking a larger share of a box someone else is using, and the width rule is kept for that reason rather than for a throughput one.
- [x] #5 #5 Main-thread deserialize cost is reported separately by the harness, since it lands on the critical path and partially cancels the win. `MeasurementRow.index_dispatch` carries the width, the boot cost, the summed worker pass time, the main-thread deserialize, the re-dispatch count and the worker-restart count, and `run_load_benchmark.ts` prints them under every arm. MEASURED 25.10 and 24.69 s over the full corpus — **31.25% of the pooled arm's whole wall**.
- [x] #6 #6 A worker crash re-dispatches its file rather than failing the load, and a cache hit is resolved before dispatch rather than by a second code path inside it. A dying worker's in-flight files go back into the queue and a replacement worker starts; a file that has killed three workers is recorded as a drop rather than retried forever, so a deterministic crasher cannot stop the load. The thread-death half cannot be staged from a fixture — a worker dies on an out-of-memory or a native fault — so `plan_worker_redispatch` carries the policy and is tested directly. Cache reuse is decided for every file before anything is dispatched, so no worker consults the cache; a blob that validates and then fails to restore goes back through the SAME dispatch rather than through a second indexing path.
- [x] #7 #7 The worker entry ships as a built `.js` in `dist` rather than relying on the host's tsx hook. `dispatch_to_workers` resolves `dist/dispatch_to_workers/worker_entry.js` — beside itself when the package is built, and up from `src` when a source-run harness dispatches — and refuses with the build command named when it is absent.
- [ ] #8 #8 The indexed grep pass and the residue grep pass dispatch through this same pool, and post-load wall on an idle box is reported with CPU unchanged within noise. NOT DONE, and no half of it is left in the tree. The pool is already reachable from `classify_entry_points` without a cross-stage back-edge — it lives in the stage-free `dispatch_to_workers/` folder for exactly that reason — but `build_grep_index` runs inside the synchronous `extract_entry_point_diagnostics`, so dispatching it turns `Project.get_call_graph` and `Project.get_classified_entry_points` async, which is a public-API change reaching `packages/mcp`. That cascade did not fit beside this step's measurement campaign, and shipping an unreachable grep pass inside the worker would have been surplus, so the worker entry indexes files and nothing else. This criterion carries forward.

<!-- AC:END -->
