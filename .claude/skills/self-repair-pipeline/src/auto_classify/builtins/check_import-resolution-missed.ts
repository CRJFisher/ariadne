// AUTO-GENERATED by triage-curator — do not edit by hand.
// Regenerate by re-running /triage-curator. Hand edits are clobbered.
//
// TypeScript named-import direct-call where Ariadne fails to link the call to the imported function. Pattern: an entry has zero resolved callers, but its grep hits include both an `import { ... }` line and a call-with-parens line in a file other than the definition file. Observed on `generateLocaleGlobalFile` in the Angular corpus, where the caller in `bin/write-locale-files-to-dist.ts` directly imports and calls the function from `../locale-global-file` and Ariadne's import resolver does not cross the `bin/` subdirectory boundary.

import type { EnrichedFunctionEntry } from "../../entry_point_types.js";
import type { FileLinesReader } from "../types.js";

function detect_language(file_path: string): string | null {
  if (file_path.endsWith(".ts") || file_path.endsWith(".tsx")) return "typescript";
  if (file_path.endsWith(".js") || file_path.endsWith(".jsx")) return "javascript";
  if (file_path.endsWith(".py")) return "python";
  if (file_path.endsWith(".rs")) return "rust";
  return null;
}

export function check_import_resolution_missed(
  entry: EnrichedFunctionEntry,
  read_file_lines: FileLinesReader,
): boolean {
  void read_file_lines;
  const check_0 = detect_language(entry.file_path) === "typescript";
  const check_1 = entry.diagnostics.ariadne_call_refs.length <= 0;
  const check_2 = (entry.diagnostics.grep_call_sites.length > 0 && entry.diagnostics.grep_call_sites.every((h) => h.file_path === entry.file_path)) === false;
  const check_3 = (() => { const pattern = new RegExp("^\\s*import\\s*\\{"); return entry.diagnostics.grep_call_sites.some((h) => pattern.test(h.content)); })();
  const check_4 = (() => { const pattern = new RegExp("\\("); return entry.diagnostics.grep_call_sites.some((h) => pattern.test(h.content)); })();
  return check_0 && check_1 && check_2 && check_3 && check_4;
}
