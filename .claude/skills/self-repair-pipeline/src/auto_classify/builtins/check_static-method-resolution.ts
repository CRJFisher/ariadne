// AUTO-GENERATED by triage-curator — do not edit by hand.
// Regenerate by re-running /triage-curator. Hand edits are clobbered.
//
// Static method invoked on an imported class via `ClassName.method(...)`. Ariadne records the call sites in the registry but the resolver fails at name_resolution with name_not_in_scope because it does not bind the imported class identifier in the caller's scope when used as a value (left of dot). Discriminators: diagnosis is `callers-in-registry-unresolved`, the call-ref's receiver_kind is `identifier`, the resolution_failure.reason is `name_not_in_scope`, at least one grep hit shows a PascalCase identifier dot-call shape, and the callers count is at least 1 (rules out entries with no call-site evidence).

import type { EnrichedFunctionEntry } from "../../entry_point_types.js";
import type { FileLinesReader } from "../types.js";

export function check_static_method_resolution(
  entry: EnrichedFunctionEntry,
  read_file_lines: FileLinesReader,
): boolean {
  void read_file_lines;
  const check_0 = entry.diagnostics.diagnosis === "callers-in-registry-unresolved";
  const check_1 = entry.diagnostics.ariadne_call_refs.some((r) => r.receiver_kind === "identifier");
  const check_2 = entry.diagnostics.ariadne_call_refs.some((r) => r.resolution_failure !== null && r.resolution_failure.reason === "name_not_in_scope");
  const check_3 = (() => { const pattern = new RegExp("\\b[A-Z][A-Za-z0-9_]*\\s*\\.\\s*[A-Za-z_][A-Za-z0-9_]*\\s*\\("); return entry.diagnostics.grep_call_sites.some((h) => pattern.test(h.content)); })();
  const check_4 = entry.diagnostics.ariadne_call_refs.length >= 1;
  return check_0 && check_1 && check_2 && check_3 && check_4;
}
