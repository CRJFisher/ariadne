// AUTO-GENERATED by triage-curator — do not edit by hand.
// Regenerate by re-running /triage-curator. Hand edits are clobbered.
//
// JavaScript static methods called via a CommonJS require binding (e.g. `const Template = require('./Template'); Template.toPath(...)`). Ariadne indexes the definition and grep sees the `ClassName.method(` call site, but the resolver does not bind the `Template` identifier — the result of `require('./Template')` — back to the module's class, so the static method's inbound call list is empty. Detection: JavaScript entry, zero resolved callers, at least one grep hit matching the `static <name>(` definition shape, and at least one grep hit matching a `CapitalizedIdentifier.method(` call shape.

import type { EnrichedFunctionEntry } from "../../types.js";
import type { FileLinesReader } from "../types.js";

function detect_language(file_path: string): string | null {
  if (file_path.endsWith(".ts") || file_path.endsWith(".tsx")) return "typescript";
  if (file_path.endsWith(".js") || file_path.endsWith(".jsx")) return "javascript";
  if (file_path.endsWith(".py")) return "python";
  if (file_path.endsWith(".rs")) return "rust";
  return null;
}

export function check_static_method_call_resolution(
  entry: EnrichedFunctionEntry,
  read_file_lines: FileLinesReader,
): boolean {
  void read_file_lines;
  const check_0 = detect_language(entry.file_path) === "javascript";
  const check_1 = entry.diagnostics.ariadne_call_refs.length <= 0;
  const check_2 = entry.diagnostics.grep_call_sites.some((h) => new RegExp("^\\s*static\\s+[a-zA-Z_$][a-zA-Z0-9_$]*\\s*\\(").test(h.content));
  const check_3 = entry.diagnostics.grep_call_sites.some((h) => new RegExp("\\b[A-Z][A-Za-z0-9_$]*\\s*\\.\\s*[a-zA-Z_$][a-zA-Z0-9_$]*\\s*\\(").test(h.content));
  return check_0 && check_1 && check_2 && check_3;
}
