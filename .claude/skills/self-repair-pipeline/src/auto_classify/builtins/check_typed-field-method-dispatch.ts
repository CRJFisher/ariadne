// AUTO-GENERATED by triage-curator — do not edit by hand.
// Regenerate by re-running /triage-curator. Hand edits are clobbered.
//
// Matches methods whose inbound call sites are `this.<field>.<method>(...)` where the resolver knows the receiver is `self_keyword` but fails with `member_type_unknown` because it cannot walk the field's declared class type to look up the method. Positive examples: `Logger.debug` (entry 0) called via `this.logger.debug(...)` where `logger = new Logger(...)`, and `MicroservicesModule.register` (entry 1) called via `this.microservicesModule.register(...)` where `microservicesModule = new MicroservicesModule()`. Negative examples: `Injector.loadController` (entry 2) is `callers-not-in-registry` with zero call refs (different failure shape — generic-parameter field type `TInjector extends Injector`, call site never indexed as a method ref); `KafkaParser.parse` (entry 3) is a name collision with `JSON.parse(...)` — all 25 call refs resolve to identifier-receiver `JSON.parse`, failure reason `name_not_in_scope`, unrelated to typed-field dispatch.

import type { EnrichedFunctionEntry } from "../../entry_point_types.js";
import type { FileLinesReader } from "../types.js";

export function check_typed_field_method_dispatch(
  entry: EnrichedFunctionEntry,
  read_file_lines: FileLinesReader,
): boolean {
  void read_file_lines;
  const check_0 = entry.diagnostics.ariadne_call_refs.some((r) => r.receiver_kind === "self_keyword");
  const check_1 = entry.diagnostics.ariadne_call_refs.some((r) => r.resolution_failure !== null && r.resolution_failure.reason === "member_type_unknown");
  return check_0 && check_1;
}
