// AUTO-GENERATED by triage-curator — do not edit by hand.
// Regenerate by re-running /triage-curator. Hand edits are clobbered.
//
// Webpack-style registry-dispatched methods: an `apply` or `serialize` method defined on a class under lib/dependencies, lib/serialization, or lib/container that has zero resolved callers. Real invocations flow through a constructor-keyed Map registry (e.g. `dependencyTemplates.get(constructor).apply(...)` in JavascriptGenerator, or `jsTypes.get(ctor).serialize(...)` in ObjectMiddleware). Ariadne indexes both caller and callee files but cannot trace the indirect association through the Map.get()-then-method-call dispatch shape.

import type { EnrichedFunctionEntry } from "../../types.js";
import type { FileLinesReader } from "../types.js";

function detect_language(file_path: string): string | null {
  if (file_path.endsWith(".ts") || file_path.endsWith(".tsx")) return "typescript";
  if (file_path.endsWith(".js") || file_path.endsWith(".jsx")) return "javascript";
  if (file_path.endsWith(".py")) return "python";
  if (file_path.endsWith(".rs")) return "rust";
  return null;
}

export function check_dynamic_dispatch(
  entry: EnrichedFunctionEntry,
  read_file_lines: FileLinesReader,
): boolean {
  void read_file_lines;
  const check_0 = detect_language(entry.file_path) === "javascript";
  const check_1 = entry.diagnostics.ariadne_call_refs.length <= 0;
  const check_2 = new RegExp("^(apply|serialize)$").test(entry.name);
  const check_3 = new RegExp("/lib/(dependencies|serialization|container)/").test(entry.file_path);
  return check_0 && check_1 && check_2 && check_3;
}
