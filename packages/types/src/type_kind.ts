/**
 * Type kind enumeration for type registry
 */
export enum TypeKind {
  CLASS = "class",
  INTERFACE = "interface",
  TYPE = "type",
  ENUM = "enum",
  TRAIT = "trait",
  PRIMITIVE = "primitive",
  UNKNOWN = "unknown"
}