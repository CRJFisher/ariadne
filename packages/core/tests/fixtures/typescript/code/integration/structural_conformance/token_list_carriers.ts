// Two schedulers sharing the token list's vocabulary, implementing nothing - token_list_carriers.ts

export class TimerScheduler {
  add(token: string): void {
    void token;
  }

  remove(token: string): void {
    void token;
  }
}

export class PendingTasks {
  add(token: string): void {
    void token;
  }

  remove(token: string): void {
    void token;
  }
}
