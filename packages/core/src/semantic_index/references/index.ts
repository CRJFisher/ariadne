/**
 * References Module - Public API
 *
 * Only exports the main processing function and its return type.
 * All specialized processing is internal.
 */

export { process_references } from "./references";
export type { ProcessedReferences } from "./reference_types";