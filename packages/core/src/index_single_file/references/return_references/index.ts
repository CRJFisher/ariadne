/**
 * Return References - Internal module
 *
 * Not exposed publicly. Used internally by references.ts
 */

// Internal exports for references.ts only
export { process_return_references } from "./return_references";
export type { ReturnReference } from "./return_references";